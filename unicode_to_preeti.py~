#! /usr/bin/env python
"""
	Author: Prashant Subedi
	This script is meant to be used as a part of file to file conversion function for unicode to preeti convertor.
"""
import unicode_preeti
import make_preeti_docx
status=open("function_chooser.nct","r")
a=status.read()
print(a)
if(a=="1st stage complete"):
	make_preeti_docx.make_preeti_docx()
else:
	unicode_preeti.unicode_preeti()
